apiVersion: v1
kind: Service
metadata:
  name: biovalidator
  namespace: biovalidator-dev
  labels:
    app.kubernetes.io/name: biovalidator
    app.kubernetes.io/component: validator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: biovalidator
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3020
